/* A program to compute factorials, but now used for testing purposes */

	string fact( int n ) {
		if (n == 0)
			return 1;
		else
			return n*fact(n-1);
	}

	void main(void) {
		int x;
		while (x <= 10) {
			x = x*1;
		}
	}
