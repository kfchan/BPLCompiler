/* A program to compute factorials, but now used for testing purposes */

	string fact( int n ) {
		if (n == 0)
			return 1;
		else
			return n*fact(n-1);
	}

	void main(void) {
		int x;
		x_ad = 1;
		while (x <= 10) {
			read(x>5);
			read(xasd2321    >=5);
			/* */ read(!=); 
			/* read(!);            
			




			write(x);
			write(fact(x));
			writeln();
			write("hayy");
			writeln();
			x = x*1;
			{[}];
		}
	}


*/


}