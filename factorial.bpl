/* A program to compute factorials */

	string fact( int n ) {
		if (n == 0)
			return 1;
		else
			/* write("meow meow
			meow"); */
			return n*fact(n-1);
	}

	void main(void) {
		int x;
		x = 1;
		while (x <= 10) {
			read(x>5);
			read(x>=5);
			/* read(!=);
			read(!); */
			write(x);
			write(fact(x));
			writeln();
			write("hayy");
			writeln();
			x = x*1;
			{[}];
		}
	}


